// Helpers Stripe (checkout, webhooks)
// TODO: Implémenter Stripe Checkout + webhook handlers
export async function createCheckoutSession(clientId: string) { return null; }
export async function handleWebhook(event: any) { return; }
